{
  "name": "@alyldas/pixel-flags",
  "version": "1.0.0",
  "description": "Pixel-perfect country flags with a flag-icons-like CSS API.",
  "author": "alyldas",
  "homepage": "https://alyldas.github.io/pixel-flags/",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alyldas/pixel-flags.git"
  },
  "bugs": {
    "url": "https://github.com/alyldas/pixel-flags/issues"
  },
  "type": "module",
  "style": "./css/pixel-flags.css",
  "sideEffects": [
    "./css/pixel-flags.css",
    "./css/pixel-flags.min.css"
  ],
  "exports": {
    "./css/*": "./css/*",
    "./flags/*": "./flags/*"
  },
  "files": [
    "css",
    "flags",
    "CHANGELOG.md",
    "README.md",
    "NOTICE.md",
    "LICENSE"
  ],
  "scripts": {
    "clean": "node scripts/clean.js",
    "build": "npm run build:package && npm run build:site",
    "build:package": "node scripts/build-package.js",
    "build:site": "node scripts/build-site.js",
    "draw:flags": "node scripts/draw-pixel-flags.js",
    "draw:flags:preview": "node scripts/draw-pixel-flags.js --draft",
    "coverage": "node scripts/coverage.js",
    "assemble:pages": "node scripts/assemble-pages.js",
    "smoke": "node --test test/smoke.test.js",
    "check:assets": "node scripts/check-assets.js",
    "check:docs": "node scripts/check-docs.js",
    "check:package": "node scripts/check-package.js",
    "check:provenance": "node scripts/check-provenance.js",
    "generate:provenance": "node scripts/generate-provenance.js",
    "lint": "eslint scripts test site-src",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "hooks:install": "husky",
    "commitlint": "commitlint --edit",
    "test": "npm run test:package && npm run test:site",
    "test:package": "node --test --test-concurrency=1 test/package-build.test.js test/iso-coverage.test.js test/package.test.js",
    "test:site": "node --test test/site.test.js test/social-card.test.js",
    "test:all": "npm test && npm run smoke",
    "verify:static": "npm run format:check && npm run lint",
    "verify:assets": "npm run check:assets && npm run check:provenance",
    "verify:package": "npm run verify:static && npm run verify:assets && npm run coverage && npm run check:docs && npm run test:package && npm run check:package",
    "verify:site": "npm run build:site && npm run test:site",
    "verify": "npm run verify:package && npm run verify:site",
    "prepublishOnly": "npm run verify && npm run smoke"
  },
  "keywords": [
    "css",
    "flags",
    "icons",
    "pixel-art",
    "png"
  ],
  "license": "MIT",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "packageManager": "npm@10.9.7",
  "engines": {
    "node": ">=22.22.2"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.3",
    "@commitlint/config-conventional": "^20.5.3",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.3.0",
    "globals": "^17.6.0",
    "husky": "^9.1.7",
    "i18n-iso-countries": "^7.14.0",
    "playwright": "^1.59.1",
    "prettier": "^3.8.3",
    "sharp": "^0.34.5"
  }
}
